# Change: 增加时间戳接口

## 为什么（Why）
当前系统缺少一个简单的时间戳查询接口，客户端无法快速获取服务器当前时间，用于时间同步或调试目的。

## 变更内容（What Changes）
- 新增 HTTP 接口 `/timestamp`，返回当前服务器时间戳
- 返回 JSON 格式，包含 Unix 时间戳和可读时间字符串

## 影响范围（Impact）
- 新增一个接口，不影响现有功能
- 无外部依赖
- 无数据存储需求
