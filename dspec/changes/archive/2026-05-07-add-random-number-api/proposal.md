# Change: 增加随机数接口

## 为什么（Why）
项目需要一个能生成随机 12 位数字的接口，供调用方获取随机数字串（如订单号、验证码等场景）。

## 变更内容（What Changes）
- 新增 HTTP 接口 `GET /random`，返回一个随机的 12 位纯数字字符串
- 在 `main.go` 中注册路由并实现处理函数

## 影响范围（Impact）
- 新增路由，不影响现有接口
- 无外部依赖，使用标准库 `math/rand` 或 `crypto/rand`
