# Design: 监听端口修改为 10001

## 技术方案
直接修改 `main.go` 中的硬编码端口字符串，无需引入配置文件或环境变量——本次是明确的端口统一调整，不是为了支持可配置化。

## 模块结构
- `main.go`：唯一改动文件，修改两处：
  - `http.Server{Addr: ":9999"}` → `":10001"`
  - `log.Println("服务启动，监听 :9999")` → `":10001"`

## 关键决策
- 不引入常量或配置项：变更范围明确，两行改动，过度抽象无收益
