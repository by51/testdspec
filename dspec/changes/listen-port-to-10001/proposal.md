调用技能: # Change: 监听端口修改为 10001

## 为什么（Why）
当前服务监听端口为 9999，需统一调整为 10001 以符合新的端口规划。

## 变更内容（What Changes）
- `main.go` 中 `http.Server.Addr` 从 `:9999` 改为 `:10001`
- 启动日志中的端口说明从 `:9999` 改为 `:10001`

## 影响范围（Impact）
- 影响模块：`main.go`（HTTP 服务入口）
- 所有依赖 9999 端口的客户端、反向代理、防火墙规则需同步更新
- 无 API 接口变动，仅端口变更